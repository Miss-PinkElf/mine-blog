# Codex CLI 文件变更展示规范交接

## 基础信息

- 创建时间：2026-07-16
- mission：`codex-cli-file-diff-guidance`
- 当前阶段：Close
- handoff 编号：001
- 是否 superseded：否

## 当前目标

- 已完成：通过仓库指令提升 Codex CLI 中 `Added` / `Edited` 结构化文件差异的可见性。

## 当前进度

- `AGENTS.md` 已新增“文件编辑与变更展示”约束。
- 已完成范围验证：`git diff --check -- AGENTS.md` 通过。
- 已提交 `51035b8`（补充 Codex 文件变更展示约束），且只包含 `AGENTS.md` 和 `.devflow/codex-cli-file-diff-guidance/`。

## 本轮完成内容

- [x] 分析截图中 Shell 写入与补丁编辑（apply_patch）导致的显示差异。
- [x] 获得用户确认后，采用“优先补丁编辑 + Shell 例外兜底”方案。
- [x] 建立轻量 DevFlow（Light DevFlow）记录、计划与任务清单。
- [x] 更新 `AGENTS.md` 并完成范围校验。
- [x] 完成本次交接文档与下一会话提示词。

## 关键决策与原因

| 决策 | 备选方案 | 原因 |
| --- | --- | --- |
| 优先使用补丁编辑（apply_patch），保留 Shell 例外 | 完全禁止 Shell 写入；增加 Hook（hook） | 兼顾结构化差异展示与生成、二进制、批量工具等合理场景；Hook 也不能保证 Shell 写入被 CLI 渲染成内联差异。 |

## 关键文件 / 产物

| 文件 | 作用 | 相关性 |
| --- | --- | --- |
| `AGENTS.md` | 持久化文件编辑与变更展示约束 | 核心实现 |
| `plans/2026-07-16-codex-cli-file-diff-guidance-plan.md` | 已执行的轻量计划 | 追溯 |
| `decision-log.md` | 方案取舍与原因 | 追溯 |
| `learnings.md` | 截图问题的技术结论 | 复用 |

## 风险 / 阻塞项 / 开放问题

- 无阻塞、无未讨论议题、无明确延期项。
- 已知限制：即使遵循本规则，Shell 写入仍不保证 Codex CLI 显示 `Added` / `Edited`。

## 立即下一步

1. 新对话如有新需求，创建或恢复对应 mission；无需继续本 mission。

## 恢复指引

1. 先读取 `state.md`。
2. 再读取 `checkpoints.md` 与 `handoffs/index.md`。
3. 如需理解本次决策，读取 `decision-log.md`、`learnings.md` 与本 handoff。

## 可从活跃上下文移除的内容

- 截图逐张比对过程、官方手册请求返回 HTTP 403 的细节，以及 PowerShell 命令输出的原始内容已不影响后续执行。
