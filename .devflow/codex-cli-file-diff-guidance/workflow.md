# Codex CLI 文件变更展示规范

- 路径：轻量路径（Mini Align → Light Plan → Light Tasks → Apply → Verify → Close）
- 当前阶段：Close（已提交）
- 目标：通过仓库指令提高 Codex CLI 中结构化文件差异（Added / Edited）的出现概率。
- 边界：只更新 `AGENTS.md`；不修改 Codex CLI 配置、不安装 Hook（钩子）、不承诺 Shell 写入也能显示内联差异。
