# 决策记录

## 2026-07-16：采用“优先补丁编辑 + Shell 例外兜底”

- 决策：在 `AGENTS.md` 中新增工具约束，普通文本编辑优先使用补丁编辑（apply_patch）；生成文件、二进制文件或批量工具等例外可使用 Shell 写入，但需展示 `git diff -- <相对路径>`。
- 原因：补丁编辑会向 Codex CLI 提供结构化变更，能够渲染 `Added` / `Edited`；Shell 命令不保证产生相同的界面效果。
- 不采用：完全禁止 Shell 写入或用 Hook（hook）强制拦截，避免阻断合理自动化并增加无效复杂度。
