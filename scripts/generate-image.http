### OpenAI Responses 协议 - 图像生成
### VS Code REST Client 插件可用
###
### 使用前:
### 1. 安装扩展: REST Client (humao.rest-client)
### 2. 在下方 @apiKey 填入你的 key（不要提交到 git）
### 3. 点 Send Request

@baseUrl = https://shell.wyzlab.ai/v1
@apiKey = sk-nD7apzYjBAqdKTAVZkpwiYG5qJ04aIWJavSbFlR28nqcxXeZ
@model = gpt-image-2

### 文生图（Responses + image_generation 工具）
# @name generateImage
POST {{baseUrl}}/responses
Authorization: Bearer {{apiKey}}
Content-Type: application/json

{
  "model": "{{model}}",
  "input": "一只在樱花树下喝茶的橘猫，插画风格",
  "tools": [
    {
      "type": "image_generation",
      "action": "generate"
    }
  ]
}

###
### 说明:
### - 响应通常是 JSON，图片在 output 里以 base64 形式返回
### - REST Client 不会自动预览图片
### - 想直接落盘 png，请用同目录的 generate-image.sh
###
